
### 🧩 Задача

**LeetCode 53. Maximum Subarray**

Фокус:

- работа с массивом;
    
- аккуратная логика;
    
- без сложных алгоритмов (Kadane можно дать интуитивно).
    

Если не хватает времени:

- разобрать устно или оставить на ДЗ.
    
```

Дан целочисленный массив nums, найдите подмассив с наибольшей суммой и верните его сумму

Example 1:
Input: nums = [-2,1,-3,4,-1,2,1,-5,4]
Output: 6
Explanation: The subarray [4,-1,2,1] has the largest sum 6.

Example 2:
Input: nums = [1]
Output: 1
Explanation: The subarray [1] has the largest sum 1.

Example 3:
Input: nums = [5,4,-1,7,8]
Output: 23
Explanation: The subarray [5,4,-1,7,8] has the largest sum 23.

```

```

class Solution:
    def maxSubArray(self, nums):
        cur_max, max_till_now = 0, -inf
        for c in nums:
            cur_max = max(c, cur_max + c)
            max_till_now = max(max_till_now, cur_max)
        return max_till_now

```

---

https://leetcode.com/problems/single-number/description/

---
## **Связные списки (Linked Lists): теория + практика**

⏱ Длительность: **1 час**

---

## 🎯 Цели занятия

- понимать **внутреннее устройство связного списка**;
    
- знать **временные и пространственные сложности** операций;
    
- понимать, **когда связный список лучше массива, а когда хуже**;
    
- уверенно работать с `ListNode`, `next`, `head`, `tail`;
    
- уметь решать базовые задачи на связные списки без паники.
    

---

# ⏱ Структура занятия

## 1️⃣ Введение и связь с прошлым материалом (5 минут)

Короткий разговор:

- чем массив плох?
    
    - дорогие вставки;
        
    - необходимость непрерывной памяти;
        
- зачем вообще нужны связные списки?
    

Зафиксировать идею:

> Связный список = данные + ссылки, **нет random access**, но дешёвые вставки/удаления

---

## 2️⃣ Теория: устройство связного списка (15 минут)

### 2.1 Что такое связный список (5 минут)

Объяснить **без кода**, на схемах / словах:

- узел (`node`);
    
- значение (`val`);
    
- ссылка (`next`);
    
- `head`, `tail`;
    
- односвязный vs двусвязный (кратко).
    

Важно проговорить:

- элементы **не лежат подряд в памяти**;
    
- каждый элемент знает только про следующий.
    

---

### 2.2 Сложности операций (5 минут)

Сравнить с массивом:

|Операция|Связный список|Массив|
|---|---|---|
|Доступ по индексу|O(n)|O(1)|
|Поиск элемента|O(n)|O(n)|
|Вставка в начало|O(1)|O(n)|
|Вставка в середину|O(1)*|O(n)|
|Удаление|O(1)*|O(n)|

* при наличии указателя на нужный узел.

Подчеркнуть:

> **Вставка/удаление дешёвые только если мы уже дошли до нужного места**

---

### 2.3 Типичные ошибки и ловушки (5 минут)

Разобрать устно:

- потеряли `next`;
    
- забыли сохранить временную ссылку;
    
- перепутали порядок операций;
    
- не обработали `None`.
    

---

## 3️⃣ Типовые приёмы и паттерны (5 минут)

Без кода, но концептуально:

- проход по списку (`while current`);
    
- фиктивная (dummy) вершина;
    
- два указателя:
    
    - быстрый / медленный;
        
    - предыдущий / текущий.
        

Обязательно сказать:

> **90% задач на списки — это аккуратная работа с указателями**

---

## 4️⃣ Практика: задачи с LeetCode (25 минут)

### 🧩 Задача 1 — демонстрация от преподавателя (10 мин)

**LeetCode 206. Reverse Linked List**

Фокус:

- пошаговый разворот;
    
- сохранение `next`;
    
- обновление указателей.
    

Проговорить вслух:

- что хранится в каждой переменной на каждом шаге;
    
- почему порядок операций важен.
    

```

[206. Reverse Linked List](https://leetcode.com/problems/reverse-linked-list/)

Дан "head" односвязного списка, переверните список и верните перевернутый список.

Example 1:
https://assets.leetcode.com/uploads/2021/02/19/rev1ex1.jpg
Input: head = [1,2,3,4,5]
Output: [5,4,3,2,1]

Example 2:
https://assets.leetcode.com/uploads/2021/02/19/rev1ex2.jpg
Input: head = [1,2]
Output: [2,1]

Example 3:
Input: head = []
Output: []

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        ...

```

```

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        prev_node = None
        curr_node = head

        while curr_node:
            next_node = curr_node.next
            curr_node.next = prev_node
            prev_node = curr_node
            curr_node = next_node
        
        return prev_node

```

---

### 🧩 Задача 2 — совместно (10 мин)

**LeetCode 21. Merge Two Sorted Lists**

Фокус:

- dummy node;
    
- аккуратное сравнение;
    
- сбор нового списка.
    

Обратить внимание:

- мы **не создаём новые узлы**, а переиспользуем существующие;
    
- память O(1).
    
```

Вам даны заголовки двух отсортированных связанных списков list1 и list2.

Объедините два списка в один отсортированный список. Список должен быть создан путем объединения узлов первых двух списков.

Верните head объединенного связанного списка.

Example 1:
https://assets.leetcode.com/uploads/2020/10/03/merge_ex1.jpg
Input: list1 = [1,2,4], list2 = [1,3,4]
Output: [1,1,2,3,4,4]

Example 2:
Input: list1 = [], list2 = []
Output: []

Example 3:
Input: list1 = [], list2 = [0]
Output: [0]

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        ...

```

```

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        head = ListNode()
        node = head

        while list1 and list2:
            if list1.val < list2.val:
                node.next = list1
                list1 = list1.next
            else:
                node.next = list2
                list2 = list2.next
            
            node = node.next

        node.next = list1 or list2

        return head.next

```
---

### 🧩 Задача 3 

**LeetCode 141. Linked List Cycle**

Фокус:

- идея fast & slow pointers;
    
- без сложной математики;
    
- проверка на `None`.
    

Если времени не хватает:

- разобрать идею устно;
    
- отдать на ДЗ.
    

```

По данному head, началу связанного списка, нужно определить, есть ли в связанном списке цикл.

В связанном списке есть цикл, если в списке есть какой-либо узел, к которому можно снова перейти, непрерывно следуя по next указателю. Значение pos используется для обозначения индекса узла, на который указывает next. Обратите внимание, что pos не передается в качестве параметра.

Верните значение true, если в связанном списке есть цикл. В противном случае верните значение false.

Example 1:
https://assets.leetcode.com/uploads/2018/12/07/circularlinkedlist.png
Input: head = [3,2,0,-4], pos = 1
Output: true
Explanation: There is a cycle in the linked list, where the tail connects to the 1st node (0-indexed).

Example 2:
https://assets.leetcode.com/uploads/2018/12/07/circularlinkedlist_test2.png
Input: head = [1,2], pos = 0
Output: true
Explanation: There is a cycle in the linked list, where the tail connects to the 0th node.

Example 3:
https://assets.leetcode.com/uploads/2018/12/07/circularlinkedlist_test3.png
Input: head = [1], pos = -1
Output: false
Explanation: There is no cycle in the linked list.

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        ...

```

```

# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        
        # Use slow-fast method to find loop O(N) time complexity
        slow = fast = head
        
        # Find loop (fast is faster, so it will reach end first if there is an end)
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            
            # If fast reached slow, there is a loop
            if fast == slow:
                return True
            
        return False

```

---

## 5️⃣ Итоги + домашнее задание (5 минут)

### Итоги занятия

- понимает **как физически работает linked list**;
    
- знает сложности операций;
    
- не боится `ListNode`;
    
- понимает, что главное — **указатели и аккуратность**.
    

---

### Домашнее задание

206 — Reverse Linked List
https://leetcode.com/problems/reverse-linked-list/

21 — Merge Two Sorted Lists
https://leetcode.com/problems/merge-two-sorted-lists/

141 — Linked List Cycle
https://leetcode.com/problems/linked-list-cycle/

83 — Remove Duplicates from Sorted List
https://leetcode.com/problems/remove-duplicates-from-sorted-list/

203 — Remove Linked List Elements
https://leetcode.com/problems/remove-linked-list-elements/

876 — Middle of the Linked List
https://leetcode.com/problems/middle-of-the-linked-list/

234 — Palindrome Linked List
https://leetcode.com/problems/palindrome-linked-list/

Статья с задачками по бинарному поиску
https://github.com/BrandonBian/leetcode/blob/main/problems-and-solutions/LeetCode/binary-search.md

---

Записи с занятия:

```

# Дан целочисленный массив nums, найдите подмассив с наибольшей суммой и верните его сумму

# Example 1:
# Input: nums = [-2,1,-3,4,-1,2,1,-5,4]
# Output: 6
# Explanation: The subarray [4,-1,2,1] has the largest sum 6.

# Example 2:
# Input: nums = [1]
# Output: 1
# Explanation: The subarray [1] has the largest sum 1.

# Example 3:
# Input: nums = [5,4,-1,7,8]
# Output: 23
# Explanation: The subarray [5,4,-1,7,8] has the largest sum 23.

# l = 0
# r = 1
# s = nums[0]
#[-2,1,-3,4,-1,2,1,-5,4]

# Example 4:
# [-4, -1, -2, -10]

# Example 5:
# [1, 2, 3, 4, 5]

# [3, -10, 5, ...]
#      -7



# [206. Reverse Linked List](https://leetcode.com/problems/reverse-linked-list/)

# Дан "head" односвязного списка, переверните список и верните перевернутый список.
# Example 1:
# https://assets.leetcode.com/uploads/2021/02/19/rev1ex1.jpg
# Input: head = [1,2,3,4,5]
# Output: [5,4,3,2,1]

# Example 2:
# https://assets.leetcode.com/uploads/2021/02/19/rev1ex2.jpg
# Input: head = [1,2]
# Output: [2,1]

# Example 3:
# Input: head = []
# Output: []

# Definition for singly-linked list.
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

# prevNode -> currentNode -> nextNode

class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        currentNode = head
        prevNode = None
        while currentNode:
          nextNode = currentNode.next
          currentNode.next = prevNode
          prevNode = currentNode
          currentNode = nextNode
        return prevNode



```